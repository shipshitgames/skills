# Session 2026-06-03

## Done

- Created `shipshitgames/skills` with 8 game-dev skills (initial commit on `master`).
- Restructured to match the `shipshitdev/skills` convention: `skills/` library + `.agents/`
  operating system + `.claude`/`.codex` symlinks + `AGENTS.md` + `.codex/instructions.md`.
- Reused the maintenance meta-skills (`skill-validator`, `skill-auditor`, `readme-sync`)
  and `SKILL-STANDARDS.md` / `PLATFORM-ADAPTATIONS.md` from `shipshitdev/skills`.
- Added spec-compliant `metadata:` frontmatter (version/tags/author) to all 8 skills.
- Wrote `.agents/memory/` (MEMORY, CONTEXT, ARCHITECTURE, DO-AND-DONT).

## Decisions

- Imperative Three.js, not R3F. One repo per game. `@shipshit/engine` its own MIT repo.
- Paid layer = one-time lifetime All Access; games + engine + skills are MIT.
- Game #2 name leaning **DEADLANE**; Game #1 still choosing (DOOMSCROLL / MURDERHOLE / MEATGRINDER).

## Next

- Push to GitHub (`shipshitgames/skills`) — awaiting the go-ahead.
- Extract `@shipshit/engine` from `fpsdemo` (next build step).
- Per-repo `.agents/` + curated skills for the game repos and the monorepo; the monorepo
  pulls TypeScript/React/Turborepo/shadcn skills from `shipshitdev/skills`.
- Optional: port the marketplace/bundle/CI machinery (plugin.json, `.claude-plugin/`,
  bundle generation) from `shipshitdev/skills`.
